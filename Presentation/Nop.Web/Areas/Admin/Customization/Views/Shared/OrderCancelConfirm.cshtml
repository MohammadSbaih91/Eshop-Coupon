@model ActionConfirmationModel

<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <h4 class="modal-title" id="@(Model.WindowId)-title">@T("Admin.Common.AreYouSure")</h4>
        </div>
        <div class="modal-body">
            @if (!string.IsNullOrEmpty(Model.AdditonalConfirmText))
            {
                <div class="additional-text">
                    @T(Model.AdditonalConfirmText)
                </div>
            }
            <div class="form-group" style="margin-bottom:15px;">
                <div class="col-md-12">
                    <input id="cancelreason" name="cancelreason" class="form-control text-box single-line" type="text" value="">
                </div>
            </div>
            @T("Admin.Common.ActionConfirmation")
        </div>
        <div class="modal-footer">
            <button type="submit" id="@(Model.WindowId)-submit-button" class="btn btn-primary pull-right">
                @T("Admin.Common.Yes")
            </button>
            <span class="btn btn-default pull-right margin-r-5" data-dismiss="modal">@T("Admin.Common.NoCancel")</span>
        </div>
    </div>
</div>